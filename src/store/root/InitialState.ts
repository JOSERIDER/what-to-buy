import { RootStateInterface } from "@/models/store";

/**
 * @name initialRootState
 * @description Instance of root state with default values.
 */
export const initialRootState: RootStateInterface = {
  isLoading: false,
};
