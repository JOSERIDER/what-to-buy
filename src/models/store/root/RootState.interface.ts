/**
 * Represent the root state of application.
 * @name RootStateInterface
 */
export interface RootStateInterface {
  /**
   * Indicate if application is loading. will be true if application is loading or false instead
   */
  isLoading: boolean;
}
